/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.graphassets.com', 'via.placeholder.com', 'cdn.sanity.io'],

    },


}

module.exports = nextConfig
